// Scaling sweep: flat loop emitting 1000 single notes -> 1000 events.
// Isolates linear lowering expansion and O(E log E) MIDI serialization as event count grows.
tempo 120;
signature 4/4;

track melody using piano {
    loop (1000) {
        play C4;
    }
}
